# This file is rewritten in place by deploy/rolling-deploy.sh on every
# deployment (it flips between port 3000 and 3001) and Nginx is told to
# reload afterward. What's committed here is just the initial state.
upstream app_backend {
    server 127.0.0.1:3000;
}
